# Codex App Computer Use 실행 지시서

> 문서 인코딩: UTF-8 with BOM / Windows CRLF

## 목표

현재 로그인된 Notion 워크스페이스에 `신청해 지원금 관리` 템플릿을 구축한다.

## 구현 기준

같은 폴더의 `신청해_지원금_Notion_설계서.md`를 유일한 기준으로 사용한다.

## 사용자 확정 조건

- 정주비: 500,000원
- 학습공간 지원비: 200,000원
- 총 지원금: 700,000원
- 두 지원금은 동시에 사용할 수 있다.
- 단, 정주비와 학습공간 지원비의 사용액과 잔액을 각각 분리해서 관리한다.

## 중요 규칙

1. 기존 페이지를 삭제하거나 수정하지 않는다.
2. 최상위에 새 페이지 `신청해 지원금 관리`를 만든다.
3. 동일 이름이 있으면 `신청해 지원금 관리 v2`로 만든다.
4. 모든 Formula 입력 후 샘플 값으로 결과를 검증한다.
5. Relation 및 Rollup 연결을 실제로 확인한다.
6. 한글이 깨지면 작업을 중단하고 파일을 UTF-8로 다시 연다.
7. 공유 및 공개는 사용자의 확인 전까지 활성화하지 않는다.

## 구현 순서

1. 메인 페이지 생성
2. 지원금 설정 DB 생성
3. 사용계획서 DB 생성
4. 지출결의서 DB 생성
5. Relation 연결
6. Rollup 연결
7. Formula 입력
8. 데이터베이스 Template 생성
9. Button 생성
10. View 생성
11. 지원 가능·불가 기준 페이지 생성
12. 증빙자료 안내 페이지 생성
13. 샘플 데이터 입력
14. 계산 검증
15. 완료 보고

## 반드시 검증할 샘플 값

- 정주비 사용액: 52,000원
- 학습공간 사용액: 120,000원
- 총 사용액: 172,000원
- 정주비 잔액: 448,000원
- 학습공간 잔액: 80,000원
- 총 잔액: 528,000원
- 전체 사용률: 약 24.6%

## 구현상 주의

Notion 단일 DB의 Rollup만으로 카테고리 누적합과 개별 항목 한도를 계산하기 어렵다면 다음 중 하나를 사용한다.

1. `카테고리 예산` 보조 DB를 추가
2. 카테고리별 Linked View의 Sum을 시각적 검증에 사용
3. 구현하지 못한 자동 검증은 작업 로그에 명시

가능하면 `카테고리 예산` 보조 DB를 추가한다.

권장 행:

- 주거비 / 500000
- 교통비 / 400000
- 교육비 / 400000
- 식비 / 200000
- 학습공간 / 200000

교통비 중 선불교통카드 100,000원 한도는 세부유형 기준으로 별도 경고 Formula를 만든다.

## 완료 보고

- 생성 페이지
- 생성 DB
- Relation과 Rollup
- Formula
- View
- Button
- 샘플 검증 결과
- 구현하지 못한 항목
- 공개 전 사용자 확인 항목
